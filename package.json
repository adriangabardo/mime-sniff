{
  "name": "mime-sniff",
  "version": "0.0.1",
  "description": "Package for mime sniffing files",
  "main": "index.js",
  "homepage": "https://github.com/adriangabardo/mime-sniff",
  "repository": {
    "type": "git",
    "url": "https://github.com/adriangabardo/mime-sniff.git"
  },
  "scripts": {
    "start": "npm run build:local && node dist/Samples/sample.js",
    "build:local": "tsc --build tsconfig.local.json",
    "build:dist": "tsc --build tsconfig.build.json",
    "copy": "copyfiles ./package.json dist && copyfiles README.md dist",
    "test": "jest"
  },
  "author": "Adrian Gabardo",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "@types/node": "^14.0.27",
    "copyfiles": "^2.3.0",
    "jest": "^26.4.0",
    "ts-jest": "^26.2.0",
    "typescript": "^3.9.7"
  }
}
